
sudo: false
language: node_js
addons:
  ssh_known_hosts: 47.115.60.210
node_js:
  - '10'
before_install:
  - openssl aes-256-cbc -K $encrypted_bdb480de655a_key -iv $encrypted_bdb480de655a_iv -in scripts/cc.enc -out scripts/cc -d
  - eval "$(ssh-agent -s)"
  - chmod 600 scripts/cc
  - ssh-add scripts/cc
  - npm i npminstall -g
install:
  - npminstall
script:
  - npm run ci
after_script:
  - ssh paxiong@47.115.60.210 'cd /opt && mkdir sssssss && echo 1111'