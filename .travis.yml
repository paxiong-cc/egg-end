sudo: false
language: node_js
addons:
  ssh_known_hosts: 47.115.60.210
node_js:
  - '10'
before_install:
  - openssl aes-256-cbc -K $encrypted_41ffe5457d1a_key -iv $encrypted_41ffe5457d1a_iv -in scripts/remote.enc -out scripts/remote -d
  - eval "$(ssh-agent -s)"
  - chmod 600 scripts/remote
  - ssh-add scripts/remote
  - npm i npminstall -g
install:
  - npminstall
script:
  - npm run ci
after_script:
  - ssh paxiong@47.115.60.210 'cd /opt && mkdir sssssss && echo 1111'