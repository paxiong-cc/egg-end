sudo: false
language: node_js
addons:
  ssh_known_hosts: 47.115.60.210
node_js:
  - '10'
before_install:
  - openssl aes-256-cbc -K $encrypted_00926c40438f_key -iv $encrypted_00926c40438f_iv -in scripts/addr.enc -out scripts/addr -d
  - eval "$(ssh-agent -s)"
  - chmod 600 scripts/addr
  - ssh-add scripts/addr
  - npm i npminstall -g
install:
  - npminstall
script:
  - npm run ci
after_script:
  - ssh paxiong@47.115.60.210 'cd /opt && mkdir sssssss && echo 1111'